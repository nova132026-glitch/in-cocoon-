# PRD v1.0：破茧——推荐算法与信息茧房互动实验室

## 0. 文档定位

本文档用于在正式开发前统一产品、视觉、互动、技术实现和交付标准。网页、演示视频和答辩 PPT 都应围绕本 PRD 和《项目需求总控》《项目总脚本》执行。

当前阶段：**核心需求已冻结，可进入视觉设计和网页开发。**

本版本冻结的关键决定：

- 作品形式为平面 2D 交互式网页；
- 主视觉为“信息温室 + 几何花朵”；
- 视频固定在左侧约三分之一；
- 信息温室位于中间，是视觉中心；
- AI 原理、标签、兴趣画像和推荐原因位于右侧；
- 视频直接使用预设主题标签，不做真实视频元素识别；
- 通过有限的点赞、查看、划走和不感兴趣操作，展示信息茧房的形成与破茧；
- 项目目的为培养 AI 素养，不鼓励沉迷或无限刷内容。

## 1. 产品定位

### 1.1 一句话定位

一件让中学生亲手体验“我的行为如何改变推荐”的交互式人工智能通识教育资源。

### 1.2 作品形态

- 电脑浏览器运行的交互式网页；
- 16:9 桌面展示优先，建议以 1440×900 作为设计基准；
- 无登录、无后端账号、无真实用户数据；
- 所有内容和推荐结果均为教学模拟；
- 支持从开场直接进入实验，也支持重置后重复演示。

### 1.3 目标对象

初中 7—8 年级学生。学生不需要理解代码，只需要通过操作看懂行为、画像和推荐之间的关系。

## 2. 核心体验目标

学生完成体验后，应能用自己的话回答：

1. 我做了什么行为？
2. 系统从这些行为中猜到了什么？
3. 为什么下一轮内容变了？
4. 内容为什么可能越来越单一？
5. 我可以怎样让信息来源更多元？

## 3. 核心创意：把算法变化变成“看得见的状态变化”

本项目不只展示卡片排序，而是让整个画面随着推荐状态变化：

- 初始状态：五类主题颜色均衡，画面开放、明亮；
- 兴趣形成：被偏好的主题逐渐变亮、变大、靠近中心；
- 茧房形成：主导主题形成半透明的“信息茧”环，其他主题缩小并退到外围；
- 主动探索：新主题进入时，颜色和粒子重新扩散，茧环出现裂缝并消散；
- 完成挑战：画面从单一状态恢复为多色、均衡、开放的状态。

视觉变化必须服务于教学，不能只是装饰。任何动效都要让学生更容易理解“推荐正在变化”。

## 3A. 必须实现的两段连贯过程

本项目不是只展示“点赞后推荐变化”，而是要让学生先看懂**内容怎样被 AI 表示**，再看懂**用户怎样被 AI 建模和推荐**。

### 过程一：视频带着内容标签进入推荐系统

```text
短视频 + 预设主题标签
        ↓
标签作为内容信号
        ↓
与用户兴趣进行匹配
        ↓
参与下一轮推荐排序
```

页面直接在视频角落显示预设标签，例如“运动、校园、户外”。标签是内容数据的一部分，不再额外做视频元素提取，也不宣称系统正在真实识别视频。

页面需要让学生知道：

1. 每条视频可以带有多个主题标签；
2. 不同标签有不同权重；
3. 标签越相似的视频，越可能被系统放在一起推荐；
4. 这些标签会直接参与后续的推荐计算。

页面提示语建议使用：

> 为了便于学习，本实验给每条内容预先设置了主题标签，用来模拟推荐系统使用的内容信号。

### 过程二：从用户行为到推荐

```text
内容主题标签
        +
点赞 / 查看 / 划走 / 屏蔽
        ↓
用户兴趣权重
        ↓
内容与用户的匹配分
        ↓
下一轮推荐排序
        ↓
推荐内容可能变得单一
```

页面必须展示：

1. 学生对内容进行点赞、查看、划走或屏蔽；
2. 右侧兴趣画像随行为变化；
3. 系统解释“为什么下一轮推荐了这条内容”；
4. 推荐流中相似主题逐渐增多；
5. 内容多样性下降，进入信息茧房风险状态；
6. 学生主动探索后，推荐主题重新变得多元。

### 两个过程的连接点

视频上的主题标签必须实际进入后续推荐计算：

```text
这条视频被标记为“运动 0.72”
        ↓
你点赞了它
        ↓
系统提高了你对“运动”的兴趣权重
        ↓
下一轮更容易推荐运动内容
```

这条链路是本作品区别于普通信息流页面的核心，也是视频和答辩必须重点展示的内容。

## 4. 视觉方向建议

### 4.1 总体风格

建议方向：**明亮的未来学习实验室 + 轻量数据可视化 + 友好的内容卡片**。

避免：仿小红书、过度赛博朋克、黑底霓虹满屏、复杂 3D、信息密度过高的后台仪表盘。

### 4.2 画面结构

采用固定三栏布局：

- 左侧约 30%—33%：短视频、视频标签和用户操作；
- 中间约 42%—45%：信息温室、几何花朵、相似内容聚集和茧房变化；
- 右侧约 22%—25%：AI 原理解释、推荐原因、兴趣画像和内容多样性；
- 底部横跨全屏：操作按钮、阶段提示和破茧挑战。

视频、温室和解释区一开始都在同一页面中。随着实验推进，中间区域的状态变化最明显；视频始终保留为当前内容来源。

### 4.3 视觉层级

- 一级：当前实验问题、内容卡片、推荐变化；
- 二级：兴趣画像、推荐原因、多样性指标；
- 三级：操作说明、数据标签、素材来源提示；
- 所有按钮都要有明确的文字，不能只依赖图标。

### 4.4 配色待确认

正式实现前由团队确认：

- 背景色；
- 主色；
- 主题色组；
- 强调色；
- 警示/茧房状态色；
- 文字与边框对比度。

建议主题色组保持五类可区分，但亮度和饱和度统一。配色确定后写入 `DESIGN.md`，网页、PPT 和视频统一使用。

## 5. 页面与流程

### 页面 A：开场页

显示：

- “为什么越刷越像？”；
- 一句话说明实验目标；
- “开始实验”按钮；
- “这是一个教学模拟，不是真实平台算法”的小提示。

动效：标题轻微进入，五个主题色点从四周汇入，点击开始后转入信息流。

### 页面 B：初始推荐流

显示：

- 8—10 张混合主题卡片；
- 每张卡的主题、标题、摘要和操作按钮；
- 右侧兴趣画像处于“尚未形成”的中性状态；
- 顶部显示实验阶段“1/3：先看看你会选择什么”。

交互：点赞、查看、划走、不感兴趣/屏蔽。

### 页面 B1：视频标签说明面板

作为初始推荐流中的展开状态或独立步骤，展示视频标签如何参与推荐：

- 视频播放区；
- 视频角落直接显示预设标签；
- 侧边解释标签权重和共同标签；
- 展示“相似标签越多，越可能被推荐在一起”；
- “进入推荐实验”按钮。

视频保持左侧主栏，标签说明位于右侧解释区；不另做真实视频识别。

### 页面 C：兴趣形成与推荐变化

显示：

- 重新排序后的推荐卡片；
- “为什么推荐给你”解释卡；
- 兴趣主题权重柱状图或环图；
- “内容多样性”指标；
- 当前推荐状态标签。

页面需要保留上一阶段的内容标签来源，让学生知道推荐变化不是凭空发生的。

动效：高权重主题卡片出现时有轻微聚拢，高权重颜色加深；动效不能影响阅读和操作。

### 页面 D：信息温室与信息茧房状态

显示：

- 五类主题以不同颜色的几何花朵表示；
- 点赞产生水滴，相关花朵长大并增加花瓣；
- 相似内容逐渐靠近同一朵花；
- 透明玻璃边界从两侧和上方逐步形成；
- 主导主题形成的半透明边界/温室；
- 其他主题退到外围；
- 简单提醒：“你现在看到的内容主要集中在 1—2 类主题。”；
- “查看推荐原因”按钮。

注意：文案使用“可能形成”“内容变得单一”等准确表达，不宣称算法必然操控用户。

### 页面 E：破茧挑战

显示：

- “探索新主题”；
- “管理兴趣偏好”；
- “重置画像”三个入口，至少实现其中两个；
- 挑战目标：“让推荐流重新出现至少 4 类主题”。

交互：探索不同主题后，推荐多样性逐步提升；茧环裂开并淡出，颜色重新扩散。

### 页面 F：完成与反思

显示：

- “挑战完成：推荐内容变得更多元”；
- 实验前后对比；
- 1—2 道反思题；
- “重新实验”按钮；
- 三句学习结论。

## 6. 互动设计

### 6.1 行为规则

| 行为 | 教学含义 | 推荐影响 |
| --- | --- | --- |
| 点赞 | 强正反馈 | 主题权重明显增加 |
| 查看/停留 | 弱正反馈 | 主题权重小幅增加 |
| 划走 | 弱负反馈 | 主题权重略微降低 |
| 不感兴趣/屏蔽 | 强负反馈 | 主题权重明显降低 |
| 探索新主题 | 主动干预 | 增加低权重主题曝光 |
| 重置画像 | 清除实验状态 | 回到初始多元状态 |

### 6.2 视觉反馈

- 每次行为都立即显示一句人话反馈；
- 点赞显示水滴飞向对应主题花朵，花朵按主题权重生长；
- 查看/停留显示柔和光线，划走使花朵暂停，不感兴趣使花朵变淡；
- 相似内容增加时为主题花朵增加花瓣或内容卡片；
- 信息茧房形成时玻璃墙和玻璃顶逐步闭合；
- 破茧时玻璃边界打开，外部主题重新进入；
- 右侧画像同步变化；
- 推荐原因必须能追溯到最近的行为；
- 不用复杂数字解释所有细节，先让学生看懂因果。

### 6.3 可重复演示

- 提供重置按钮；
- 页面初始状态固定；
- 推荐结果采用确定性规则，不使用每次完全不同的随机结果；
- 演示者可以在 60 秒内跑完整条故事线。

## 7. 推荐模型设计

### 7.1 最小模型

每个主题维护一个权重：

```text
推荐分 = 主题兴趣权重 × 行为强度 + 少量探索分
```

推荐逻辑不接入真实平台或外部 AI API。只用本地 JSON 内容和浏览器内存状态即可完成。

### 7.2 内容标签数据结构

每条视频或内容卡至少包含以下字段：

```json
{
  "title": "校园骑行",
  "tags": ["运动", "校园", "户外"],
  "topics": {
    "运动": 0.72,
    "自然": 0.54,
    "校园": 0.60,
    "科技": 0.18,
    "艺术": 0.05
  }
}
```

`tags` 是预设的内容信号，直接用于计算内容相似度；`topics` 表示内容与不同主题的关联程度，并参与推荐匹配。

### 7.3 建议默认参数

- 初始五个主题权重相同；
- 点赞：+3；
- 查看/停留：+1；
- 划走：−1；
- 屏蔽：−4；
- 探索补偿：给低权重主题额外 +2；
- 推荐结果始终保留少量探索内容。

参数最终应通过试用调整，不能让“信息茧房”形成得太慢，也不能一次点赞就完全锁死。

## 8. 技术栈建议

### 8.1 推荐方案

- 前端：React + TypeScript；
- 构建：Vite/Vinext 项目结构，优先复用已有 Sites 工程能力；
- 状态：React `useReducer` 或单一状态对象；
- 数据：本地 `content_cards.json`；
- 图表：优先使用 SVG/CSS 自绘，避免引入过重的图表库；
- 动效：CSS transition + 少量 `motion`/GSAP；先保证稳定，再增加动效；
- 图标：开源图标组件或 CSS 图形，不使用真实平台图标；
- 部署：静态网页/无后端部署；
- 录屏：固定 1440×900 桌面视口，保证视频画面一致。

### 8.2 不做的技术

- 不做账号、数据库、登录和权限；
- 不接真实推荐 API；
- 不训练真实机器学习模型；
- 不做实时多人协作；
- 不做 3D 场景和复杂 WebGL；
- 不为了“看起来像 AI”而加入不可解释的接口调用。

## 9. 需要 AI 协助的工作

### ChatGPT

- 教学文案、页面文案、视频旁白和 PPT 讲稿；
- 反思题、测试题和不同年龄段语言改写；
- 视觉方向与版式讨论；
- 从评委视角检查表达是否清楚。

### Codex

- 网页页面和交互逻辑；
- 推荐权重、内容排序、图表状态联动；
- 预置演示状态和 60 秒录屏流程；
- 项目文件、源代码、素材清单和作品说明整理；
- 视频片头、字幕、动态图表等可复用素材。

### 团队成员

- 最终定稿和取舍；
- 视觉配色确认；
- 真实试用与问题记录；
- 素材来源、AIGC 记录和原创过程证明；
- 最终录屏、剪辑、PPT 制作和答辩。

## 10. 已冻结的产品范围

正式写页面时只实现以下内容：

1. 明亮、友好的学习实验室气质；
2. 五个主题和视频标签结构；
3. 视频、标签、用户行为、推荐、温室和破茧六个功能板块；
4. 四种行为规则；
5. 至少两种破茧方式；
6. 60 秒演示路径；
7. 本地数据、简化算法和无后端实现。

不加入账号、评论、真实平台复刻、真实视频识别、复杂 3D、实时多人和真实用户数据。

## 11. 验收标准

### 功能验收

- 初始内容多元；
- 连续偏好后推荐明显收窄；
- 推荐原因可解释；
- 图表和页面状态同步变化；
- 主动探索后内容多样性恢复；
- 可以重置并重复演示；
- 60 秒内跑通完整故事。

### 视觉验收

- 配色统一、文字清楚、按钮明显；
- 动效有意义，不影响阅读；
- 茧房状态和破茧状态一眼可区分；
- 画面不会像后台管理系统，也不会像单纯卡片列表；
- 1440×900 录屏时不出现溢出、遮挡或过小文字。

### 教学验收

- 学生能说出行为、画像和推荐之间的关系；
- 学生能说出一种减少内容单一化的方法；
- 项目明确说明这是简化模拟，不冒充真实平台算法。

## 12. 视觉执行项

产品结构已经冻结。正式开始视觉设计前，只需补齐具体色值、字体和图标风格，并同步到网页、PPT 和视频。配色不改变上述布局、互动和教学流程。
